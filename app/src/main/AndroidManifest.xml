<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="cn.idu.learnvideo">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:name=".main.ExApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.LearnVideo">
        <activity android:name=".ffmpeg.FFInfoActivity"></activity>
        <activity android:name=".opengles.EGLSample02CameraActivity" />
        <activity android:name=".opengles.EGLSample01TriangleActivity" />
        <activity android:name=".mp.AudioCaptureActivity" />
        <activity android:name=".opengles.GlSample03CameraActivity" />
        <activity android:name=".opengles.GlSample02ImageActivity" />
        <activity android:name=".opengles.GlSample01TriangleActivity" />
        <activity android:name=".renderimg.RenderImageActivity" />
        <activity android:name=".mp.VideoCaptureActivity" />
        <activity android:name=".main.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>